import XCTest
@testable import DependencyProviderContainer

final class DependencyProviderContainerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DependencyProviderContainer().text, "Hello, World!")
    }
}
