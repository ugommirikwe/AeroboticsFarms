import XCTest
@testable import FarmDataSourceLocal

final class FarmDataSourceLocalTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FarmDataSourceLocal().text, "Hello, World!")
    }
}
