import XCTest
@testable import FarmDataSourceRemote

final class FarmDataSourceRemoteTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FarmDataSourceRemote().text, "Hello, World!")
    }
}
