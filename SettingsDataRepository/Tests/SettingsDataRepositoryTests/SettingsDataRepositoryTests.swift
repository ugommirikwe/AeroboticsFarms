import XCTest
@testable import SettingsDataRepository

final class SettingsDataRepositoryTests: XCTestCase {
    func testExample() throws {
        //XCTAssertEqual(SettingsDataRepository().text, "Hello, World!")
    }
}
